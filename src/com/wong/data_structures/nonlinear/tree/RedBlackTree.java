package com.wong.data_structures.nonlinear.tree;

public class RedBlackTree {
    // RedBlackTree is also self-balancing binary search tree, but without too many rotation when doing left/right rotate when required
    // and will just keep approximately balanced instead of absolute balance like AVL tree, doing rotation for absolute might
    // take more time than search the node. RBT rely on recoloring, it faster to do then rotating the tree.
}
